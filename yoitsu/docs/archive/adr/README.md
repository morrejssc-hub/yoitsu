# ADR Archive

This directory keeps ADRs that have already been accepted and absorbed into
the active architecture baseline.

Current in-flight ADRs remain in `docs/adr/`.

Use archived ADRs for:

- decision history
- implementation detail that no longer needs to stay on the active path
- rationale behind the current baseline
