REJECT

- High: the observation-review path is still incorrect for non-`budget_variance` triggers. [`ObservationThresholdEvent`](\/home\/holo\/yoitsu\/yoitsu-contracts\/src\/yoitsu_contracts\/external_events.py#L82) and the reviewer prompt ([`reviewer.md`](\/home\/holo\/yoitsu\/palimpsest\/evo\/prompts\/reviewer.md#L9)) both advertise `preparation_failure` and `tool_retry`, and [`reviewer_role`](\/home\/holo\/yoitsu\/palimpsest\/evo\/roles\/reviewer.py#L18) now forwards `metric_type`. But [`observation_context`](\/home\/holo\/yoitsu\/palimpsest\/evo\/contexts\/loaders.py#L443) ignores that value and always queries `/observation/budget_variance/*`, which matches the only Pasloe routes in [`observation.py`](\/home\/holo\/yoitsu\/pasloe\/src\/pasloe\/domains\/observation.py#L158). A `preparation_failure` threshold will therefore render budget-variance data while telling the model it is reviewing preparation failures. Issue 1/3 is not actually complete.

- Medium: `ReviewProposal.from_json_str()` is still too brittle for realistic model output. In [`review_proposal.py`](\/home\/holo\/yoitsu\/yoitsu-contracts\/src\/yoitsu_contracts\/review_proposal.py#L126) it parses only the first fenced block, then falls back to a greedy `\{[\s\S]*\}` match. I reproduced failures where analysis contains an earlier JSON snippet and a valid final proposal, and where text contains multiple inline JSON objects. That means the “analysis text followed by JSON” contract is still not reliably handled.

- Medium: `ReviewProposal.to_trigger_data()` now merges `task_template.params` with override precedence in [`review_proposal.py`](\/home\/holo\/yoitsu\/yoitsu-contracts\/src\/yoitsu_contracts\/review_proposal.py#L93), but [`TriggerData`](\/home\/holo\/yoitsu\/yoitsu-contracts\/src\/yoitsu_contracts\/events.py#L84) explicitly rejects canonical task fields inside `params`. A plausible reviewer output like `params={"repo": ...}` or `params={"goal": ...}` now produces an invalid trigger at runtime. Since the prompt does not forbid those keys, this is a new failure surface introduced by Issue 2.

- Assumption: [`prompts/reviewer.md`](\/home\/holo\/yoitsu\/palimpsest\/evo\/prompts\/reviewer.md) is currently untracked in `palimpsest/evo`. If that file is not added before commit, [`reviewer_role`](\/home\/holo\/yoitsu\/palimpsest\/evo\/roles\/reviewer.py#L30) will fall back to the literal string `prompts/reviewer.md` as the system prompt.

- Verification: `pytest palimpsest/tests/test_observation_context.py`, `pytest tests/test_review_proposal.py` in `yoitsu-contracts`, and `pytest tests/test_observation_threshold.py` in `trenni` all passed. The new `yoitsu-contracts` test does not collect cleanly when invoked from the monorepo root.