REJECT

- Medium: the `to_trigger_data()` fix is still incomplete for canonical task fields. [`review_proposal.py:118`](/home/holo/yoitsu/yoitsu-contracts/src/yoitsu_contracts/review_proposal.py#L118) only filters `goal|budget|repo|...`, but canonical fields like `role` and `team` are still allowed through `task_template.params`, even though `TriggerData` documents `team` as a top-level field and says `params` should be role-internal only in [`events.py:60`](/home/holo/yoitsu/yoitsu-contracts/src/yoitsu_contracts/events.py#L60) and [`events.py:61`](/home/holo/yoitsu/yoitsu-contracts/src/yoitsu_contracts/events.py#L61). I reproduced this with `TaskTemplate(params={"team": "backend"})`: the resulting trigger validates and keeps contradictory data (`team="default"` at top level, `params["team"]="backend"`). The claimed “forbidden keys filtered from params” fix is therefore only partial.

- Medium: unsupported observation metrics still fail open instead of being rejected or surfaced clearly. [`ObservationThresholdEvent.metric_type`](/home/holo/yoitsu/yoitsu-contracts/src/yoitsu_contracts/external_events.py#L96) is still an unconstrained `str`, [`observation_threshold_to_trigger()`](/home/holo/yoitsu/yoitsu-contracts/src/yoitsu_contracts/external_events.py#L250) passes it through unchanged, and [`observation_context()`](/home/holo/yoitsu/palimpsest/evo/contexts/loaders.py#L487) only handles `budget_variance|preparation_failure|tool_retry`, with no fallback for anything else. For a typoed event like `unknown_metric`, the reviewer gets only the section header plus `Metric filter: unknown_metric`, with no data and no warning. That is a silent correctness failure in the trigger-to-review path.

Open question / process risk:
- `yoitsu-contracts` still has essential new files untracked in `git status` (`src/yoitsu_contracts/review_proposal.py`, `tests/test_review_proposal.py`). If this is intended to ship, make sure they are actually committed in that subrepo; otherwise the import added in [`__init__.py`](/home/holo/yoitsu/yoitsu-contracts/src/yoitsu_contracts/__init__.py#L30) will not exist in the landed patch.

Verification:
- I ran `pytest yoitsu-contracts/tests/test_review_proposal.py`, `pytest palimpsest/tests/test_observation_context.py`, `pytest palimpsest/tests/test_roles.py`, and `pytest yoitsu-contracts/tests/test_observation_events.py`; all passed.